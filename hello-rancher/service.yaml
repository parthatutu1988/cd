apiVersion: v1
kind: Service
metadata:
  name: hello-rancher
  namespace: hello-rancher
spec:
  type: NodePort
  selector:
    app: hello-rancher
  ports:
    - name: web
      port: 8080
      targetPort: web
      nodePort: 30000
